const { test, expect } = require('@playwright/test'); const ROOT=process.env.RUNG_UI_BASE_URL; test.use({launchOptions:{executablePath:process.env.RUNG_CHROMIUM_PATH}});
test('mobile transfer reconciliation comparison and choices are usable',async({page})=>{await page.setViewportSize({width:390,height:844});await page.goto(ROOT,{waitUntil:'networkidle'});await page.locator('#authEmail').fill('transfer-browser@example.com');await page.locator('#authPassword').fill('browser-pass-123');await page.locator('#authLoginBtn').click();await page.locator('.overview-quiet-details summary').click();const list=page.locator('#reconciliationList');await expect(list).toContainText('Possible duplicate transfer');await expect(list).toContainText('Browser match');await expect(list.getByRole('button',{name:'Match'}).first()).toBeVisible();await expect(list.getByRole('button',{name:'Keep Separate'}).first()).toBeVisible();expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBe(true);expect(await list.evaluate(el=>el.scrollHeight>=el.clientHeight)).toBe(true);});
